"""Credit-score scrapers (Phase 4.3).

Mirrors the offers-scraper architecture: each portal owns a ``site_key``,
auth-state JSON files persist the logged-in browser session, and the
coordinator runs all enabled scrapers on a daily cron.
"""
