"""Scraper modules for ingest-by-crawl data sources.

Right now this package contains only ``legal_claims`` — class-action
settlement listings from TopClassActions / ClassAction.org. The folder
exists so future scrapers (retention offers, store-promo aggregators,
etc.) have an obvious home next to it instead of getting jammed into
``ingestion`` which is reserved for first-party feeds (Plaid, CSV/OFX,
Gmail).
"""
